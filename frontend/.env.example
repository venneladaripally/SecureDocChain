# URL of the backend API (Express server).
# Local dev: http://localhost:5000
# Production (Vercel): set this in Vercel's Project Settings -> Environment
# Variables to your deployed backend's URL, e.g. https://your-backend.onrender.com
VITE_API_URL=http://localhost:5000